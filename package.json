{
  "name": "design-patterns",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.7",
    "prettier": "^2.6.2",
    "tslint": "^6.1.3",
    "typescript": "^4.6.3"
  },
  "lint-staged": {
    "*.ts": [
      "prettier --write",
      "git add"
    ]
  }
}
